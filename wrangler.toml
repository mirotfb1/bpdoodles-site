name = "bpdoodles-site"
compatibility_date = "2026-05-19"

# Static-assets-only Worker — no server code.
# (The /subscribe endpoint and worker.js were removed when the newsletter stopped.)
[assets]
directory = "."
exclude = ["wrangler.toml", "WORKFLOW.md"]
not_found_handling = "404-page"

[observability]
enabled = true
